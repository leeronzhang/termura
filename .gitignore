# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcuserstate
build/
DerivedData/

# Swift Package Manager
.build/
*.resolved

# Generated Xcode project
*.xcodeproj/

# Tests (internal)
Tests/

# Harness paid implementation (private — stubs in Sources/Termura/Harness/ are public)
# Real implementations live in the private termura-harness Swift Package.
# The following patterns cover files that will exist there but must never be tracked here:
# HarnessViewModel.swift, RuleFileRepository.swift, RuleFileParser.swift,
# CorruptionDetector.swift, ExperienceCodifier.swift, RuleSection*.swift (impl only)

# CI/CD (internal)
.github/

# Internal build tooling (not required to use the app)
Makefile
scripts/
project.yml
.swiftlint.yml
.swiftformat

# CI/CD (internal)
.github/

# Internal docs
docs/

# Claude Code
CLAUDE.md
.claude/

# Landing page (separate repo)
landing/

# macOS
.DS_Store
.termura/
